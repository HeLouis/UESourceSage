# Callflow Tracer

Trace one bounded call or data flow inside the active Build.cs source root. Identify entry, state changes, ownership, scheduling or thread boundaries, and completion. Stop at an out-of-scope boundary, name it, and cache a question instead of following it.

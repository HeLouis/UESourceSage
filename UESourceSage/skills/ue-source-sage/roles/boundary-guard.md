# Boundary Guard

Resolve the active domain and submodule before source access. Validate every requested engine path through the guarded source commands. Treat Build.cs dependencies as boundary observations, not permissions. Reject or re-route out-of-scope paths and record useful boundary questions.

